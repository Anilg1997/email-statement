# Database
DATABASE_URL=postgresql+asyncpg://bankuser:bankpass@localhost:5432/bank_editor

# Server
HOST=0.0.0.0
PORT=8080

# Security
SECRET_KEY=change-me-to-a-random-secret-key

# SMTP (optional — configure via UI)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_NAME=Bank Statement Service
